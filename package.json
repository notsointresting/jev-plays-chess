{
  "name": "jev-plays-chess",
  "version": "0.1.0",
  "description": "Play chess against Jev — every legal move is one option of a single typed Choice question. No engine, no generated text. Free, browser-based, BYOP, powered by Pollinations.",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "node --check config.js && node --check auth.js && node --check jev.js && node --check board.js && node --check app.js",
    "serve": "python -m http.server 8000"
  },
  "license": "MIT"
}
